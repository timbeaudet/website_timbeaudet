---
title: 'mDevelopmentLog[4] = More Networking'
author: timbeaudet
type: post
date: 2017-05-30T21:38:49+00:00
url: /2017/05/30/devlog_w04/
sfw_pwd:
  - CQNGpqC50FsY
categories:
  - Game Development
  - Programming
  - TurtleBrains

---
Hello Turtles,

So last week Monky bird and Sara catÂ commandedÂ me to fiddle with adding a networking layer to TurtleBrains and thus wasÂ to be continued for a second week, to ensure that the networking layer could be used on Windows as well as Mac and Linux. Long ago I remember a teacher or someone experienced talk about how the sockets in WinSock are straight-forward and based on sockets used in unix systems. For the most part that is true, but<!--more-->, notÂ the complete story.

The Windows socket API tends to return SOCKET\_ERRORÂ during a failure and then a call to WSAGetLastError() for more information. That is similar to the unix style, which returns -1 on error and errno / strerror(errno) for more information. That strerror trick is amazing. If one assumes that SOCKET\_ERROR is, **and will always be**, defined as -1, then the same code could theoretically work on both. I did dig through the headers and that is theoretically a safe assumption, however, I didn&#8217;t assume it will always be that way.

[cpp]  
//Example of handling an error Windows vs Mac/Linux.  
const int selectReturn = select(0, &set, 0, 0, &timeoutValue);

#if defined(tb_windows)  
if (SOCKET_ERROR == selectReturn)  
{  
const int errorCode = WSAGetLastError();  
tbi\_log\_error("tbNetwork Error: Select error: %d\n", errorCode);  
return false;  
}  
#elif defined(tb\_macosx) || defined(tb\_linux)  
if (-1 == selectReturn)  
{  
const int errorCode = errno;  
tbi\_log\_error("tbNetwork Error: Select error: %d (%s)\n", errorCode, strerror(errno));  
return false;  
}  
#endif  
[/cpp]

So that is what it looks like on a basic level. Speaking of select, the return value on both apisÂ is to be non-zero if one of the socketsÂ within set have incoming data to be handled.Â I was unable to figure out why, no obvious errors, but TCP sockets on Mac/Linux were never collecting incoming data, selectÂ always returned 0.

It was actually quite difficult to stay motivated and focused on the networking layer of TurtleBrains.Â Partially because during the previous week I realized the API was going to be remaining unstable for quite some time. The test case I had setup was not &#8220;real-world&#8221; enough to be comfortable defining a stable/usable interface. So while it works on a basic level,Â it probablyÂ isn&#8217;t done well enough for real use.Â I knew some people wanted to try the networking and that didÂ **actually** help my motivation to get it working on Mac/Linux. Unfortunately onlyÂ TCP is still not working, though the rest is,Â _on that basic level._

[<img loading="lazy" decoding="async" class="aligncenter size-full wp-image-404" src="/blog/assets/2017/05/egg_drop_example_720.jpg" alt="" width="720" height="405" srcset="/blog/assets/2017/05/egg_drop_example_720.jpg 720w, /blog/assets/2017/05/egg_drop_example_720-300x169.jpg 300w" sizes="auto, (max-width: 720px) 100vw, 720px" />][1]

Moku also gave me the final art for theÂ EggDrop Tutorial for TurtleBrains, and it looks great. I went to export the source art into png files toÂ drop into the game and foundÂ thatÂ InkScape didn&#8217;t load the svg files properly,Â missing paths/objects and such.Â Potentially some optionsÂ Moku has inÂ saving, but not quite sure, maybe next week.

Until then, have a good one!

 [1]: /blog/assets/2017/05/egg_drop_example_720.jpg
